server:
    port: 8761

eureka:
    instance:
        hostname: eurekaServer8761
    client:
        register-with-eureka: false
        fetch-registry: false
        service-url:
            defaultZone: http://localhost:8762/eureka/
    server:
        enable-self-preservation: false

spring:
    datasource:
        url: jdbc:mysql://localhost:3306/elm?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&useSSL=false
        username: root
        password: 123456
        driver-class-name: com.mysql.cj.jdbc.Driver

    # config:
    #     import: "optional:configserver:"
    cloud:
        # config:
        #     uri: http://localhost:15000